/**
 * console.firebase.google.com create a project
 * Register project
 * 
 * 
 * 1. first install firebase : npm install firebase;
 * 2. Get firebase config and do not save firebase config in client site;
 * 3. create auth = getAuth(app) 
 * 4. Export auth from firebase.init.js file.. ;
 * 5. in the console authentication > Started > enable google provider (entered email)
 * 6. 
 */